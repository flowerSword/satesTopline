# SalesTopline Python dependencies
# For offline deployment, pre-download wheels first:
#   pip download -r requirements.txt -d wheels
#   pip download torch --index-url https://download.pytorch.org/whl/cpu -d wheels
# then install with:
#   pip install --no-index --find-links wheels -r requirements.txt

# Web framework
Flask>=3.0.0,<4.0.0
Werkzeug>=3.0.0,<4.0.0

# AI semantic search (optional - app falls back to keyword search if missing)
sentence-transformers>=2.2.2
numpy>=1.24.0
torch>=2.0.0
